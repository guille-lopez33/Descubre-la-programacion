int x=150;
int y=160;
  
void dibuja(){
  rect(x,y,10,20);
  
}

void animate(){
  if (keyPressed && key == "up"){
    y=-5+y;
  }
  if (keyPressed && key == "down"){
    x=5-x;
  }
}

void main(){
dibuja();
animate();
}
